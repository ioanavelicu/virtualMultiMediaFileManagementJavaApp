package main.Menu;

/**
 * Enumeratie care cuprinde posibilele optiuni ce pot fi adaugate unui meniu*/
public enum Options {
    EXIT, BACK, ADD, REMOVE, RENAME, CHANGELANGUAGE,
    MANAGEFILES, MANAGEDIRECTORIES, LIST, MOVE, STATISTICS
}
